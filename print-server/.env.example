# Nome exato da impressora térmica no Windows (Painel → Impressoras).
# Deixe vazio para usar a impressora padrão do sistema.
# PRINTER_NAME=POS-58

# Modo desenvolvimento: salva PDF em vez de imprimir (não use no totem em produção)
# SAVE_PDF_DIR=C:\caminho\para\receipts
